const apiBaseUrl = 'http://localhost:8080';

export default Object.freeze({

  // backend urls
  INVOICES_PATH: apiBaseUrl + '/invoices',

  // variables stored in
  STORAGE_TOKEN: 'vi_card_token'
})
